# Inspiration: https://github.com/edx/configuration/blob/master/.travis.yml
env: 
  global: 
    - IPOPT_DEFAULT_LINEAR_SOLVER=ma57
    - MOSEKLM_LICENSE_FILE=/home/travis/build/testbot/restricted/mosek.lic
    
before_script:
  - set -e
  
dist: trusty
os: linux
language: generic

script:
  - sudo apt-add-repository ppa:octave/stable -y
  - sudo apt-get update -qq
  - sudo apt-get install octave liboctave-dev -y
  - sudo apt-get install -y xvfb
  - wget https://github.com/casadi/binaries/releases/download/commit-${COMMIT}/casadi-linux-octave-$COMMIT.tar.gz -O octave.tar.gz
  - ls
  - echo "casadi-octave-$COMMIT.tar.gz"
  - mkdir octave && tar -xvf octave.tar.gz -C octave
  - Xvfb :1 &
  - export DISPLAY=:1
  - octave --no-gui --eval "addpath('/home/travis/build/casadi/binaries/octave'),savepath,exit"
  - matlabgcc
  - pushd casadi/test && make octave && popd

  
branches:
  except:
    - /.*appveyor.*/

before_install:
  - pushd casadi && export COMMIT=`git rev-parse --short=7 HEAD` && popd
  - pushd ../../ && git clone https://github.com/casadi/testbot.git
  - pushd testbot && source recipes/setup.sh && popd
  - sudo apt-get install p7zip-full -y
  - export SLURP_OS=trusty
  - slurp_common_test
  - popd
  - sudo apt-get install -y libslicot0 binutils git cmake liblapack3 ipython python-numpy python-scipy python-matplotlib libblas3 libxml2
  - sudo pip install pysftp
  - python --version
  - python -c "import pysftp"
  - ssh-keyscan web.sourceforge.net >> ~/.ssh/known_hosts 
