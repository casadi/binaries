# Inspiration: https://github.com/edx/configuration/blob/master/.travis.yml
env: 
  global: 
    - IPOPT_DEFAULT_LINEAR_SOLVER=ma57
    - LD_LIBRARY_PATH=/home/travis/build/testbot/restricted/worhp:/home/travis/build/snopt75/lib:/home/travis/build/mosek/7/tools/platform/linux64x86/bin/:/home/travis/build/gurobi/lib
    - CMAKE_INCLUDE_PATH=/home/travis/build/testbot/restricted/cplex/cplex/include:/home/travis/build/testbot/restricted/cplex/concert/include
    - CMAKE_LIBRARY_PATH=/home/travis/build/testbot/restricted/cplex/cplex/lib/x86-64_sles10_4.1/static_pic/:/home/travis/build/testbot/restricted/cplex/concert/lib/x86-64_sles10_4.1/static_pic/
    - ILOG_LICENSE_FILE=/home/travis/build/testbot/restricted/cplex/access.ilm
    - WORHP=/home/travis/build/testbot/restricted/worhp
    - WORHP_LICENSE_FILE=/home/travis/build/testbot/restricted/worhp/unlocked.lic
    - MOSEK=/home/travis/build/mosek
    - MOSEKLM_LICENSE_FILE=/home/travis/build/testbot/restricted/mosek.lic
    - SNOPT=/home/travis/build/snopt75
    - GUROBI_HOME=/home/travis/build/gurobi
    - DEBIAN_FRONTEND=noninteractive

os: linux
dist: trusty
language: generic

before_script:
  - set -e

matrix:
  include:
    - compiler: gcc
      services: docker
      env: PYTHONVERSION=27 NUMPYVERSION=1.9.1 TESTCOMMIT=1
      script:
        - docker run --rm "dockcross/linux-armv6" > dockcross
        - chmod +x dockcross

        - ./dockcross cc --version
        - ./dockcross c++ --version
        - ./dockcross cmake --version
        - ./dockcross ls
        - sudo ./dockcross apt-get install -y python-dev python-numpy
        
        - sudo apt-get install -y libslicot-dev binutils gcc g++ gfortran git cmake liblapack-dev ipython libmumps-seq-dev libblas-dev liblapack-dev libxml2-dev
        - sudo apt-get install -y fakeroot rpm alien
        - mkdir build
        - ./dockcross cmake -B/work/build -H/work -DWITH_OPENMP=OFF -DWITH_ECOS=OFF -DOLD_LLVM=OFF -DWITH_CLANG=OFF -DWITH_SELFCONTAINED=ON -DWITH_PYTHON=ON -DWITH_MOSEK=OFF -DWITH_WORHP=OFF -DWITH_SLICOT=OFF -DWITH_PYTHON=ON -DWITH_JSON=OFF -DWITH_IPOPT=OFF -DCMAKE_INSTALL_PREFIX=../python_install -DPYTHON_PREFIX=../python_install ..
        - export HVERSION=`python /home/travis/build/testbot/helpers/extract_hversion.py`
        - echo "::$HVERSION::"
        - ./dockcross make -C /work/build  VERBOSE=1
        - ./dockcross make -C /work/build install
        #- ldd ../python_install/casadi/libcasadi_nlpsolver_worhp.so
        - pushd python_install && tar -czf ../bundle.tar.gz . && popd
        - export RESULT_TAR="$PWD/bundle.tar.gz"
        - |
          cat <<EOF > upload.py
          import pysftp
          sftp = pysftp.Connection('web.sourceforge.net', 
                                username='casaditestbot,casadi',
                                private_key='/home/travis/build/testbot/id_rsa_travis')
          
          binaries_dir = '/home/jg/binaries'
          
          
          with sftp.cd('/home/pfs/project/c/ca/casadi/CasADi'):
            sftp.makedirs('commits')
            with sftp.cd('commits'):
              sftp.makedirs("$COMMIT")
              with sftp.cd("$COMMIT"):
                sftp.makedirs('linux')
                with sftp.cd('linux'):
                  sftp.put("$RESULT_TAR","raspberry_pi_casadi-py$PYTHONVERSION-np$NUMPYVERSION-$COMMIT.tar.gz")
          EOF
        - cat upload.py
        - python upload.py
    - compiler: gcc
      env: PYTHONVERSION=27 NUMPYVERSION=1.9.1 TESTCOMMIT=1
      script:
        - sudo apt-get install -y libslicot-dev binutils gcc g++ gfortran git cmake liblapack-dev ipython python-dev python-numpy python-scipy python-matplotlib libmumps-seq-dev libblas-dev liblapack-dev libxml2-dev
        - sudo apt-get install -y fakeroot rpm alien
        - pushd casadi
        - mkdir build
        - pushd build
        - cmake -DWITH_OPENMP=OFF -DWITH_ECOS=ON -DOLD_LLVM=ON -DWITH_CLANG=ON -DWITH_SELFCONTAINED=ON -DWITH_PYTHON=ON -DWITH_MOSEK=OFF -DWITH_WORHP=ON -DWITH_SLICOT=ON -DWITH_PYTHON=ON -DWITH_JSON=ON  -DCMAKE_INSTALL_PREFIX=../python_install -DPYTHON_PREFIX=../python_install ..
        - export HVERSION=`python /home/travis/build/testbot/helpers/extract_hversion.py`
        - echo "::$HVERSION::"
        - grep -i ipopt CMakeCache.txt
        - make VERBOSE=1
        - make install
        #- ldd ../python_install/casadi/libcasadi_nlpsolver_worhp.so
        - popd
        - export PYTHONPATH=$PYTHONPATH:`pwd`/python_install
        - python -c "from casadi.tools import *;loadAllCompiledPlugins()"
        - pushd test && make unittests_py && popd
        - pushd python_install && tar -czf ../bundle.tar.gz . && popd
        - export RESULT_TAR="$PWD/bundle.tar.gz"
        - |
          cat <<EOF > upload.py
          import pysftp
          sftp = pysftp.Connection('web.sourceforge.net', 
                                username='casaditestbot,casadi',
                                private_key='/home/travis/build/testbot/id_rsa_travis')
          
          binaries_dir = '/home/jg/binaries'
          
          
          with sftp.cd('/home/pfs/project/c/ca/casadi/CasADi'):
            sftp.makedirs('commits')
            with sftp.cd('commits'):
              sftp.makedirs("$COMMIT")
              with sftp.cd("$COMMIT"):
                sftp.makedirs('linux')
                with sftp.cd('linux'):
                  sftp.put("$RESULT_TAR","casadi-py$PYTHONVERSION-np$NUMPYVERSION-$COMMIT.tar.gz")
          EOF
        - cat upload.py
        - python upload.py
    - compiler: gcc
      env:
        - PYTHONVERSION=32
        - NUMPYVERSION=1.9.1
        - WITH_PYTHON3=ON 
      script:
        - sudo apt-get update -qq
        - sudo apt-get install -y libslicot-dev binutils gcc g++ gfortran git cmake liblapack-dev ipython python-dev python-numpy python-scipy python-matplotlib libmumps-seq-dev libblas-dev liblapack-dev libxml2-dev
        - sudo apt-get install -y fakeroot rpm alien
        - sudo apt-get install cmake valgrind libgomp1 python3-dev python3-numpy python3-scipy python3-matplotlib python3-setuptools -y
        - pushd casadi
        - mkdir build
        - pushd build
        - cmake -DWITH_ECOS=ON -DOLD_LLVM=ON -DWITH_CLANG=ON -DWITH_SELFCONTAINED=ON -DWITH_PYTHON=ON -DWITH_PYTHON3=ON -DWITH_MOSEK=ON -DWITH_WORHP=ON -DWITH_SLICOT=ON -DWITH_PYTHON=ON -DWITH_JSON=ON  -DCMAKE_INSTALL_PREFIX=../python_install -DPYTHON_PREFIX=../python_install ..
        - export HVERSION=`python /home/travis/build/testbot/helpers/extract_hversion.py`
        - echo "::$HVERSION::"
        - grep -i ipopt CMakeCache.txt
        - make VERBOSE=1
        - make install
        #- ldd ../python_install/casadi/libcasadi_nlpsolver_worhp.so
        - popd
        - export PYTHONPATH=$PYTHONPATH:`pwd`/python_install
        - python3 -c "from casadi.tools import *;loadAllCompiledPlugins()"

        - pushd python_install && tar -czf ../bundle.tar.gz . && popd
        - export RESULT_TAR="$PWD/bundle.tar.gz"
        - |
          cat <<EOF > upload.py
          import pysftp
          sftp = pysftp.Connection('web.sourceforge.net', 
                                username='casaditestbot,casadi',
                                private_key='/home/travis/build/testbot/id_rsa_travis')
          
          binaries_dir = '/home/jg/binaries'
          
          
          with sftp.cd('/home/pfs/project/c/ca/casadi/CasADi'):
            sftp.makedirs('commits')
            with sftp.cd('commits'):
              sftp.makedirs("$COMMIT")
              with sftp.cd("$COMMIT"):
                sftp.makedirs('linux')
                with sftp.cd('linux'):
                  sftp.put("$RESULT_TAR","casadi-py$PYTHONVERSION-np$NUMPYVERSION-$COMMIT.tar.gz")
          EOF
        - cat upload.py
        - python upload.py
    - compiler: gcc
      env:
        - PYTHONVERSION=34
        - NUMPYVERSION=1.9.1
        - WITH_PYTHON3=ON
      script:
        - sudo apt-get install binutils gcc g++ gfortran git cmake liblapack-dev libmumps-seq-dev libblas-dev liblapack-dev libxml2-dev -y
        - sudo apt-get install valgrind libgomp1 python3-dev python3-numpy python3-scipy python3-matplotlib python3-setuptools -y
        - pushd casadi
        - mkdir build
        - pushd build
        - cmake -DWITH_ECOS=ON -DOLD_LLVM=ON -DWITH_CLANG=ON -DWITH_SELFCONTAINED=ON -DWITH_PYTHON=ON -DWITH_PYTHON3=ON -DWITH_MOSEK=OFF -DWITH_WORHP=ON -DWITH_SLICOT=ON -DWITH_PYTHON=ON -DWITH_JSON=ON  -DCMAKE_INSTALL_PREFIX=../python_install -DPYTHON_PREFIX=../python_install ..
        - export HVERSION=`python /home/travis/build/testbot/helpers/extract_hversion.py`
        - echo "::$HVERSION::"
        - grep -i ipopt CMakeCache.txt
        - make VERBOSE=1
        - make install
        #- ldd ../python_install/casadi/libcasadi_nlpsolver_worhp.so
        - popd
        - export PYTHONPATH=$PYTHONPATH:`pwd`/python_install
        - python3 -c "from casadi.tools import *;loadAllCompiledPlugins()"
        - pushd test && make unittests_py && popd 
        - pushd python_install && tar -czf ../bundle.tar.gz . && popd
        - export RESULT_TAR="$PWD/bundle.tar.gz"
        - |
          cat <<EOF > upload.py
          import pysftp
          sftp = pysftp.Connection('web.sourceforge.net', 
                                username='casaditestbot,casadi',
                                private_key='/home/travis/build/testbot/id_rsa_travis')
          
          binaries_dir = '/home/jg/binaries'
          
          
          with sftp.cd('/home/pfs/project/c/ca/casadi/CasADi'):
            sftp.makedirs('commits')
            with sftp.cd('commits'):
              sftp.makedirs("$COMMIT")
              with sftp.cd("$COMMIT"):
                sftp.makedirs('linux')
                with sftp.cd('linux'):
                  sftp.put("$RESULT_TAR","casadi-py$PYTHONVERSION-np$NUMPYVERSION-$COMMIT.tar.gz")
          EOF
        - cat upload.py
        - python upload.py

    - compiler: gcc
      env:
        - PYTHONVERSION=35
        - NUMPYVERSION=1.9.1
        - WITH_PYTHON3=ON
      script:
        - sudo apt-get install git binutils gcc g++ gfortran cmake liblapack-dev libmumps-seq-dev libblas-dev liblapack-dev libxml2-dev -y
        - pinning xenial
        - tail /etc/apt/sources.list
        - tail /etc/apt/preferences.d/mytest
        - sudo apt-get update
        - sudo apt-get --yes --force-yes install -q -t xenial -o Dpkg::Options::="--force-confdef" -o Dpkg::Options::="--force-confnew" python3-dev python3-numpy
        - sudo apt-get install cmake valgrind libgomp1 -y
        - pushd casadi
        - mkdir build
        - pushd build
        - export CC=gcc-4.8
        - export CXX=g++-4.8
        - export FC=gfortran-4.8
        - cmake -DWITH_ECOS=ON -DOLD_LLVM=ON -DWITH_CLANG=ON -DWITH_SELFCONTAINED=ON -DWITH_PYTHON=ON -DWITH_PYTHON3=ON -DWITH_MOSEK=OFF -DWITH_WORHP=ON -DWITH_SLICOT=ON -DWITH_PYTHON=ON -DWITH_JSON=ON  -DCMAKE_INSTALL_PREFIX=../python_install -DPYTHON_PREFIX=../python_install ..
        - export HVERSION=`python /home/travis/build/testbot/helpers/extract_hversion.py`
        - echo "::$HVERSION::"
        - grep -i ipopt CMakeCache.txt
        - make VERBOSE=1
        - make install
        #- ldd ../python_install/casadi/libcasadi_nlpsolver_worhp.so
        - popd
        - export PYTHONPATH=$PYTHONPATH:`pwd`/python_install
        - python3 -c "from casadi.tools import *;loadAllCompiledPlugins()"
        #- pushd test && make unittests_py && popd # SOME ERRORS
        
        - pushd python_install && tar -czf ../bundle.tar.gz . && popd
        - export RESULT_TAR="$PWD/bundle.tar.gz"
        - |
          cat <<EOF > upload.py
          import pysftp
          sftp = pysftp.Connection('web.sourceforge.net', 
                                username='casaditestbot,casadi',
                                private_key='/home/travis/build/testbot/id_rsa_travis')
          
          binaries_dir = '/home/jg/binaries'
          
          
          with sftp.cd('/home/pfs/project/c/ca/casadi/CasADi'):
            sftp.makedirs('commits')
            with sftp.cd('commits'):
              sftp.makedirs("$COMMIT")
              with sftp.cd("$COMMIT"):
                sftp.makedirs('linux')
                with sftp.cd('linux'):
                  sftp.put("$RESULT_TAR","casadi-py$PYTHONVERSION-np$NUMPYVERSION-$COMMIT.tar.gz")
          EOF
        - cat upload.py
        - python upload.py

  
after_success:
  - test_commit tests-debian

branches:
  except:
    - /.*appveyor.*/

before_install:
  - sudo apt-get update
  - sudo apt-get install python-dev -y
  - pushd casadi && export COMMIT=`git rev-parse --short=7 HEAD` && popd
  - pushd ../../ && git clone https://github.com/casadi/testbot.git
  - pushd testbot && git checkout merged && source recipes/setup.sh && popd
  - export SLURP_OS=trusty
  - sudo apt-get install p7zip-full -y
  - slurp ecos
  - travis_retry testbot/recipes/fetch.sh gurobi650.tar.gz && mkdir gurobi && tar -xf gurobi650.tar.gz -C gurobi && rm gurobi650.tar.gz
  - slurp hsl
  - travis_retry testbot/recipes/fetch.sh snopt75.tar.gz && mkdir snopt75 && tar -xvf snopt75.tar.gz -C snopt75
  - slurp clang
  - travis_retry testbot/recipes/fetch.sh mosek7.tar.bz2 && tar -xvf mosek7.tar.bz2
  - mkdir -p /home/travis/build/casadi/testbot
  - slurp ipopt
  - slurp bonmin
  - slurp swig
  - pushd testbot/restricted/cplex && mkdir cplex && tar -xvf cplex12.4.tar.gz -C cplex && mkdir concert && tar -xvf cplex12.4-concert.tar.gz -C concert && popd
  - pushd testbot/restricted && mkdir -p worhp && cd  worhp && 7za e ../libworhp_debian7_amd64_gcc47_1.7-12970f3.7z -y && popd
  - popd
  - sudo pip install pysftp
  - ssh-keyscan web.sourceforge.net >> ~/.ssh/known_hosts 
  
